<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.benimdnyam">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/benimdunyam"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".yeraciklamaactivity"></activity>
        <activity android:name=".Editorunsecimiactivity" />
        <activity android:name=".Favorilerimactivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>